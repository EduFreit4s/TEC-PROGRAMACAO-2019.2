#include <iostream>
#include <map>

using namespace std;

void agenda(map <string, string> &copia);

void mostra_lista();

bool nome_repetido(string nome);

void exibe_repetido(string nome);

void coleta_telefone(string nome);